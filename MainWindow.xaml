<Window x:Class="TextEditorWpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TextEditorWpf"
        mc:Ignorable="d"
        Title="Text Editor" Height="450" Width="800">
    <Window.Resources>
        <Style x:Key="HoverButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#FF0E0A0A"/>
            <Setter Property="Foreground" Value="#FFEBE0E0"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="White"/>
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="InputTextBox" Grid.Row="0" Height="60" Margin="0,0,0,10" BorderBrush="#FF010102" />
        <ListBox x:Name="ParagraphListBox" Margin="10,65,10,15" BorderBrush="#FF010102" Background="#FF0E0A0A" Foreground="#FFEBE0E0" Grid.RowSpan="2"  />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Add Image" Width="100" Margin="5,0" Click="AddImage_Click" Background="#FF0E0A0A" Foreground="#FFEBE0E0"/>
            <Button Content="AddText" Width="100" Margin="5,0" Click="AddTextBlock_Click" Background="#FF0E0A0A" Foreground="#FFEBE0E0"/>
            <Button Content="Remove Text" Width="100" Margin="5,0" Click="Remove_Text" Background="#FF0E0A0A" Foreground="#FFEBE0E0"/>
            <Button Content="Remove Image" Width="100" Margin="5,0" Click="Remove_Image" Background="#FF0E0A0A" Foreground="#FFEBE0E0"/>
            <Button Content="Undo" Width="100" Margin="5,0" Click="Undo_Click" Background="#FF0E0A0A" Foreground="#FFEBE0E0"/>
            <Button Content="Exit" Width="120" Click="Exit_Click" Background="#FF0E0A0A" Foreground="#FFEBE0E0" />
        </StackPanel>


    </Grid>
</Window>
